# 俄罗斯方块开发文档 v1.9 — 增量更新说明

> 本文档仅记录相对于原始开发文档及 v1.1～v1.8 的新增、修改与调整内容。未在本文档中提及的要求继续沿用旧版本；若冲突，以 v1.9 为准。

## 1. 游戏界面操作说明中文化

游戏界面旁边的操作说明统一以中文为主：

- `← / →　左右移动`
- `↓　加速下落`
- `↑　顺时针旋转`
- `空格　直接落下`
- `C　暂存方块`
- `P / Esc　暂停`
- `U　撤回一次`（仅 Chill 且 OOPS 可用时）

SCORE、BEST、LEVEL、LINES、NEXT、HOLD、COMBO、CLASSIC、CHILL、ZEN 等街机 HUD 文字可继续保留英文，但操作说明、功能解释和提示文字优先中文。

## 2. 重构输入焦点系统

当前存在典型浏览器焦点问题：点击 Focus、Settings、Guide、音量等外部按钮后，按钮仍保持 keyboard focus，随后按 Space 可能再次触发该按钮，而不是作用于游戏。

v1.9 正式引入 Input Context，建议至少划分：

- `MENU`
- `GAME`
- `MODAL`

可选增加 `TEXT_INPUT`。

### MENU
用于主菜单、模式选择等。Space 不执行 Hard Drop。

### GAME
用于实际游戏。键盘输入优先进入游戏逻辑：
- Space → Hard Drop
- ↑ → 顺时针旋转
- ← / → → 左右移动
- ↓ → Soft Drop

### MODAL
用于 Settings、Special Guide、Pause 等。此时游戏操作暂停或屏蔽；关闭后若游戏仍运行，自动恢复 GAME。

## 3. 外部按钮点击后自动归还游戏焦点

游戏 Canvas 或游戏根容器应设置 `tabindex="0"`。

玩家在游戏进行中点击以下非持续编辑型控件后：
- Focus Mode
- HUD 开关
- 音量开关
- 一次性设置按钮
- 其他普通按钮

操作完成后，若游戏仍在运行，应立即：
- `gameRoot.focus()`
- Input Context 恢复 `GAME`

## 4. Space 键行为修正

在 GAME 状态：
- Space 只执行 Hard Drop
- 使用 `preventDefault()`
- 不允许再次触发 focused button
- 不允许页面滚动

Game Over 时：
- Space 可执行 Instant Retry

Slider、Select、文本输入等正在主动编辑时，不应粗暴截获其必要键盘行为。

## 5. 输入处理优先级

建议统一处理顺序：

1. 检查 Input Context
2. 检查是否处于 Slider / Select / 文本输入编辑状态
3. 若为 GAME，则由游戏系统处理
4. 游戏专用键调用 `preventDefault()`
5. 不依赖浏览器默认 Button 键盘行为

目标：彻底消除“玩家以为在操作游戏，实际按键还作用在网页控件上”的情况。

## 6. 旋转系统简化

双向旋转对本项目定位过于臃肿。

v1.9 要求：
- 删除逆时针旋转
- 只保留顺时针旋转
- `↑` = 顺时针旋转

删除：
- `Z`
- `[`
- `]`
- `【`
- `】`
- 其他逆时针旋转快捷键

移动端只保留一个：
- `旋转 ↻`

删除双旋转按钮。

## 7. Wall Kick 继续保留

尽管旋转简化，Wall Kick 仍保留，因为它属于容错手感，而不是操作复杂度。

要求：
- 靠墙旋转自然
- 不穿墙
- 不因单方向旋转降低基本可玩性

## 8. IRS 简化

IRS（Initial Rotation System）继续保留，但仅支持顺时针。

玩家在新方块生成前按住 `↑`，新方块生成时自动尝试顺时针旋转一次。

不再处理逆时针 IRS。

## 9. 字体体系重构

当前字体虽已放大，但整体仍偏网页感。v1.9 建立双层字体体系。

### 英文标题 / 数字 / HUD
适用于：
- RETRO BLOCKS
- SCORE
- LEVEL
- NEXT
- HOLD
- COMBO
- 分数数字
- 模式名称
- Event Card 英文副标题

推荐风格：
- Pixel
- Arcade
- Square
- Monospace
- Retro terminal

若不嵌入字体，可优先使用：
`"Lucida Console", "Courier New", monospace`

并配合：
- uppercase
- 较大 letter-spacing
- 较明显 font-weight
- 克制 text-shadow
- 方形边框和块状排版

若允许嵌入合法开源字体，可将 WOFF2 以 Base64 / `@font-face` 内嵌到单 HTML 中，保持离线运行。

### 中文字体
中文不强行使用伪像素字体。优先：
`"Microsoft YaHei UI", "PingFang SC", "Noto Sans CJK SC", sans-serif`

通过：
- 更粗字重
- 更合适字号
- 紧凑行高
- 轻微描边 / 阴影
- 中英层级

增强游戏感。

## 10. 中英混排规范

推荐：
- 中文主标题更大
- 英文副标题更小、letter-spacing 更宽

例如：

**随机惊喜**  
`SURPRISE EVENTS`

兼顾中文可读性与 Retro Arcade 氛围。

## 11. Arcade Control Skin

普通 Button、Slider、Select 会强化网页感，因此所有游戏内设置控件统一改为街机化视觉。

### 按钮
- 像素边框或方形轮廓
- 明确 pressed 状态
- hover / active / focus 有反馈
- 禁止浏览器默认蓝色按钮风格

### Slider
可视觉化为：
`◀ ███████░░ ▶`

底层仍可使用 HTML range，但外观应与游戏一致。

### 选择器
例如 Surprise Frequency：
`◀ RELAXED | NORMAL | LIVELY ▶`

尽量避免浏览器默认下拉框成为主要视觉。

## 12. 鼠标闲置自动隐藏

PC 游戏进行时新增 Auto-hide Cursor：

- GAME 状态下
- 鼠标连续约 2～3 秒未移动
- 自动隐藏鼠标指针
- 再次移动立即恢复

以下状态不隐藏：
- MENU
- Settings
- Special Guide
- Pause
- Slider 正在操作
- Modal 打开

## 13. 操作说明自动折叠

首次进入游戏显示完整操作说明。

持续游玩约 30～60 秒后，可自动折叠成：
- `操作说明 ?`
- 或 `按键说明`

玩家可手动展开 / 收起。

折叠状态可保存，但不得影响新玩家第一次进入时看到基础操作。

## 14. 三种模式共享同一套基础操作

Classic / Chill / Zen 统一：

- ← / →：左右移动
- ↓：加速下落
- ↑：顺时针旋转
- Space：直接落下
- C：Hold
- P / Esc：Pause

Chill 独有：
- U：OOPS（若启用）

避免三个模式分别维护不同基础键位。

## 15. 移动端操作按钮中文化

移动端建议统一为：
- `← 左移`
- `→ 右移`
- `↓ 加速`
- `↻ 旋转`
- `落下`
- `暂存`
- `撤回`

保持符号 + 中文的组合。

## 16. 可选：菜单键盘导航

可增加：
- 主菜单：↑ / ↓ 选择，Enter / Space 确认
- Pause Menu：↑ / ↓ 选择，Enter 确认，Esc 返回

必须由 Input Context 控制，不能与游戏操作冲突。

## 17. Focus Mode 焦点修正专项要求

测试流程必须满足：

1. 游戏运行
2. 点击 Focus
3. Focus 状态切换
4. HUD 更新
5. 自动将焦点归还 gameRoot
6. Input Context 恢复 GAME
7. 再按 Space

预期：
- 只执行 Hard Drop
- 不再次触发 Focus
- 页面不滚动

## 18. 焦点视觉样式

控件仍需保留可访问性 Focus Ring，但游戏运行时不得长期显示浏览器默认蓝色焦点框。

建议：
- 使用 `:focus-visible`
- 鼠标点击时弱化
- 键盘导航时保留清晰自定义焦点样式

## 19. 字体性能要求

若内嵌字体：
- 只嵌入必要字重
- 不嵌入巨大完整字体集
- 字体失败时正常 fallback
- 不显著拖慢单 HTML 启动速度

## 20. v1.9 新增验收测试

### 中文操作说明
- PC 游戏操作说明为中文
- 移动端按钮为中文或中文+符号
- 关键操作不依赖英文理解

### Focus / UI 焦点
- 点击 Focus 后按 Space 只执行 Hard Drop
- Music、SFX、HUD 等普通按钮操作后，游戏焦点能正确恢复
- 页面不滚动
- 不重复触发旧按钮

### Modal 输入隔离
- Settings / Guide / Pause 打开时 Space 不执行 Hard Drop
- 方向键不控制棋盘
- 关闭后自动恢复 GAME 输入

### 旋转简化
- ↑ 正常顺时针旋转
- `Z`、`[`、`]`、`【`、`】` 不再旋转
- 移动端只有一个旋转按钮
- Wall Kick 正常

### IRS
- 提前按 ↑ 可执行初始顺时针旋转
- 不存在逆时针 IRS
- 不穿墙

### 字体体系
- HUD 英文 / 数字具有明显游戏感
- 中文清晰
- 中英层级明确
- 字体失败时可正常 fallback

### Arcade Controls
- Button、Slider、Selector 不显示浏览器默认风格
- 键盘仍可操作
- Focus 样式清晰

### Cursor Auto Hide
- GAME 中 2～3 秒无鼠标移动后隐藏
- 移动立即出现
- MENU / Settings / Guide 不隐藏

### 操作说明折叠
- 初次进入显示完整说明
- 可自动或手动折叠
- 可再次展开
- 不影响棋盘尺寸和游戏逻辑


## 21. 窄屏 / 手机端必须单屏完整呈现游戏

当前问题：

- 在窄屏或手机端，游戏内容可能超过可视高度
- 玩家需要上下滑动页面才能看到棋盘、触控按钮或状态信息
- 这种行为会破坏游戏连续性，也会明显强化“网页感”

v1.9 补充要求：

**进入实际游戏后，必须尽量保证一个完整游戏界面在单个可视屏幕内完整呈现，不需要上下滑屏。**

### 21.1 实际游戏状态禁止页面纵向滚动

在 GAME 状态下：

- 页面本身不应产生纵向滚动条
- 不允许通过上下滚屏来查看棋盘或触控按钮
- 棋盘、核心 HUD 和主要触控操作必须同时处于当前可视区域内

可以使用专门的游戏根容器控制布局。

但不得因为禁止滚动而裁切关键内容。

### 21.2 Mobile Game Layout

窄屏 / 手机端不得直接把桌面三栏布局机械缩小。

必须切换成专门的 Mobile Game Layout。

推荐结构：

1. 顶部：精简状态栏
2. 中间：尽可能大的主棋盘
3. 棋盘顶部或两侧：HOLD / NEXT
4. 底部：触控按钮区
5. 次要信息折叠到 Pause / Guide / 可展开面板

核心目标：

**主棋盘和操作按钮在同一屏内同时可见。**

### 21.3 移动端信息优先级

手机端空间不足时，优先级从高到低为：

1. 主棋盘完整可见
2. 核心触控按钮完整可见
3. HOLD
4. NEXT 1
5. SCORE / LEVEL
6. 必要持续状态，例如 FREEZE
7. NEXT 2 / NEXT 3
8. Recent Surprises
9. 操作说明
10. 次级统计信息

如果空间仍不足：

- 优先隐藏 / 折叠次级信息
- 然后压缩间距
- 再适当缩小 HUD
- 最后才缩小棋盘

禁止：

为了保留所有次要信息，把主棋盘压缩得过小。

### 21.4 动态计算棋盘尺寸

棋盘尺寸应综合：

- `window.innerWidth`
- `window.innerHeight`
- 可用容器尺寸
- 顶部状态栏高度
- 底部触控按钮高度

动态计算。

必须始终保持：

- 10×20 比例
- 格子为正方形
- 棋盘完整显示

### 21.5 避免死板依赖 100vh

移动端浏览器顶部 / 底部地址栏会动态变化。

因此：

- 不建议单纯依赖固定 `100vh`
- 应优先使用实际可用高度
- 可结合 `window.innerHeight`
- 若浏览器支持，可合理使用更适合移动端动态视口的单位

目标：

避免底部触控按钮被浏览器工具栏遮挡。

### 21.6 矮屏适配

必须特别测试：

- 320px 左右窄屏
- 360×640
- 375×667
- 390×844
- 414×896
- 以及高度约 650～700px 的矮屏设备

即使高度较小：

- 仍不能依赖上下滚动完成基本游戏操作
- 需要通过紧凑布局自动适配

### 21.7 横屏适配

若手机进入横屏：

允许切换为：

**左侧棋盘 + 右侧触控 / HUD**

或其他更适合横屏的布局。

但同样要求：

- 单屏完成游戏
- 不需要页面滚动
- 主棋盘不被压缩到难以操作

---

## 22. 顶部菜单栏视觉层级重构

当前问题：

- 最上方菜单栏与普通 HUD 的视觉差异不够明显
- Pause、Settings、Focus、Guide 等系统级操作和 SCORE、NEXT、HOLD 等游戏数据容易混在一起
- 顶部区域缺乏层级感和识别度

v1.9 补充要求：

**顶部菜单栏必须成为独立的“游戏系统控制层”，与普通 HUD 明显区分。**

### 22.1 UI 层级明确化

整体界面层级应明确为：

#### Level 1：顶部系统菜单栏

包含：

- 当前模式
- Pause
- Settings
- Focus
- Guide
- 必要系统操作

视觉上属于“游戏框体控制台”。

#### Level 2：主棋盘

整个页面的绝对视觉核心。

#### Level 3：游戏 HUD

包括：

- HOLD
- NEXT
- SCORE
- BEST
- LEVEL
- LINES
- COMBO

用于辅助游戏。

#### Level 4：次级信息

包括：

- 操作说明
- Recent Surprises
- Surprise Frequency
- Audio Intensity
- 辅助提示

视觉权重最低。

### 22.2 顶部菜单栏外观

顶部栏应与普通 HUD 使用不同的视觉处理。

建议：

- 背景比普通 HUD 更深一层
- 使用明确的上 / 下像素边框
- 可增加极轻的内阴影或凸起面板感
- 与主体区域之间保留 6～12px 左右视觉分隔
- 不要做成一排普通网页按钮

目标：

让玩家一眼识别：

> 这里是系统控制区域，不是游戏状态数据。

### 22.3 当前模式标识整合进顶部栏

建议左侧显示：

- `RETRO BLOCKS · CLASSIC`
- `RETRO BLOCKS · CHILL`
- `RETRO BLOCKS · ZEN`

或等价形式。

要求：

- 当前模式明确
- 不与 SCORE / NEXT 混在一起
- 保持街机风格

### 22.4 系统按钮设计

顶部按钮例如：

- Pause
- Settings
- Focus
- Guide

应使用统一 Arcade Control Skin。

当前激活状态：

- 使用明确底板
- 使用边框变化
- 使用轻微亮度变化

不应只靠：

- 很轻的文字变色

来区分状态。

### 22.5 顶部栏与主棋盘保持距离

顶部菜单栏和主棋盘之间应有明确视觉分隔。

可使用：

- 间距
- 细分隔线
- 深浅变化
- 小型像素结构

但不得：

- 加入巨大装饰
- 抢占棋盘空间

### 22.6 PC 顶部栏

PC 端可使用：

左侧：
- 游戏名 / 当前模式

右侧：
- Pause
- Focus
- Settings
- Guide

要求：

- 信息不拥挤
- 点击区域明确
- 不影响主棋盘宽度

### 22.7 手机顶部栏

手机端不能照搬 PC 顶栏。

要求：

- 高度必须更紧凑
- 优先使用短文字或图标 + 简短标签
- 不允许顶部栏占据过多垂直空间

例如可使用：

- `Ⅱ`
- `⚙`
- `?`
- `FOCUS`

或更统一的像素图标。

详细说明可放入展开菜单。

### 22.8 顶部栏不得破坏单屏布局

手机 / 窄屏状态下：

顶部菜单栏必须参与整体高度预算。

即：

- 顶栏不能因为样式升级而进一步把棋盘挤小
- 如果空间不足，应缩短顶栏高度、减少文字、折叠次级按钮

而不是让页面重新出现纵向滚动。

---

## 23. v1.9 补充验收测试：移动端单屏完整呈现

测试实际游戏页面：

- 320px 左右宽度
- 360×640
- 375×667
- 390×844
- 414×896

要求：

- 不需要上下滑动页面即可完整游玩
- 主棋盘完整显示
- 核心触控按钮完整显示
- HOLD / NEXT 关键内容可见
- 页面无纵向滚动条
- 浏览器地址栏变化后布局仍然稳定
- 不出现底部按钮被遮挡

若屏幕高度不足：

- 次级信息正确折叠
- 核心操作不能消失

---

## 24. v1.9 补充验收测试：顶部菜单栏

要求：

- 顶部菜单栏与普通 HUD 视觉上明显不同
- 当前模式一眼可辨
- Pause / Focus / Settings / Guide 具有统一系统控制风格
- 当前激活按钮有明显状态
- PC 顶栏不挤压主棋盘
- 手机顶栏足够紧凑
- 顶栏升级后仍满足单屏游戏要求


## 25. v1.9 核心目标

v1.9 的主题：

**从“网页小游戏”进一步转向“真正的游戏界面”。**

核心调整：
- 操作说明中文化
- Input Context / Game Input Focus 重构
- 修复 Focus 后 Space 误触
- 旋转简化为单方向
- ↑ 作为唯一旋转键
- 移动端单旋转按钮
- 游戏化字体体系
- Arcade Control Skin
- 鼠标闲置自动隐藏
- 操作说明自动折叠
- 窄屏 / 手机端单屏完整呈现游戏
- GAME 状态禁止依赖页面纵向滚动
- Mobile Game Layout 与动态棋盘尺寸
- 顶部菜单栏层级重构
- 明确系统菜单 / 棋盘 / HUD / 次级信息四层视觉结构

开发实现时：

**以原开发文档 + v1.1～v1.8 为基础，并以本 v1.9 覆盖所有与输入焦点、旋转键位、操作说明语言、字体体系、控件视觉和鼠标行为相关的旧要求。**
