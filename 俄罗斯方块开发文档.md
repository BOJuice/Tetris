# RETRO BLOCKS —— 休闲复古俄罗斯方块开发文档

> 目标：生成一款完成度高、可直接双击 `index.html` 运行的网页版俄罗斯方块。它既保留经典俄罗斯方块的核心手感，也提供更轻松、有道具、有续命机制的休闲模式，适合学习或工作间隙玩 5～10 分钟。

---

## 1. 项目定位

项目名称：**RETRO BLOCKS**

核心气质：

- 鲜艳色彩
- 复古街机
- CRT 显示器质感
- 轻微霓虹
- 像素风 UI
- 轻松、解压、可随时退出
- 有完成度，但不做成复杂竞技模拟器

游戏应做到：

> 打开 → 立即明白怎么玩 → 玩几分钟 → 有爽感 → 随时暂停或关闭 → 下次继续玩也没有心理负担。

明确不要加入：

- 每日签到
- 抽卡
- 氪金
- 广告
- 体力系统
- 强制任务
- 连续登录奖励
- 复杂养成
- 排位赛
- 网络排行榜

---

# 2. 技术约束

最终只生成一个文件：

```text
index.html
```

必须满足：

- HTML、CSS、JavaScript 全部写在同一个文件中。
- 不使用 React、Vue、Svelte、TypeScript、Node.js。
- 不需要 npm。
- 不需要构建工具。
- 不需要后端。
- 尽量不依赖 CDN。
- 不依赖外部图片。
- 不依赖外部音频文件。
- 双击 HTML 即可运行。
- 离线环境下仍可正常游戏。

主棋盘使用 `HTML Canvas` 绘制。

UI 信息、按钮、菜单可使用普通 HTML。

游戏循环使用：

```js
requestAnimationFrame()
```

避免用大量 DOM 元素表示棋盘。

---

# 3. 游戏模式

主菜单提供两个模式。

## 3.1 CLASSIC MODE

标准俄罗斯方块模式。

特点：

- 使用 7 种标准 Tetromino。
- 无特殊道具。
- 有 Ghost Piece。
- 有 Hold。
- 有 Next Queue。
- 有 Combo。
- 有 Back-to-Back。
- 支持 T-Spin 识别与提示。
- 等级越高，下落速度越快。
- 堆到顶部即 Game Over。

目标：

> 保留足够纯粹的经典俄罗斯方块体验。

---

## 3.2 CHILL MODE

休闲模式。

特点：

- 基础规则与 Classic 相同。
- 整体下降速度更慢。
- 升级后的速度增长也更温和。
- 有特殊道具。
- 有一次续命。
- 有 Slow Time。
- 有更轻松的音效和 UI 提示。
- 不追求竞技难度。

目标：

> 普通玩家能比较轻松地玩 5～10 分钟，不会因为几次失误立刻结束。

---

# 4. 棋盘与 Tetromino

棋盘大小：

```text
10 列 × 20 行
```

标准 Tetromino：

- I
- O
- T
- S
- Z
- J
- L

推荐默认颜色：

| Tetromino | 颜色 |
|---|---|
| I | Cyan 青色 |
| O | Yellow 黄色 |
| T | Purple 紫色 |
| S | Green 绿色 |
| Z | Red 红色 |
| J | Blue 蓝色 |
| L | Orange 橙色 |

颜色应鲜艳，但不要刺眼。

单个方块视觉不要只是纯色矩形，应加入：

- 较亮顶部高光
- 较暗底部/边缘
- 微弱渐变
- 像素边框
- 极轻微 Glow

确保玩家能迅速区分各种方块。

---

# 5. 7-Bag Randomizer

必须实现 **7-Bag Randomizer**。

规则：

1. 将 I/O/T/S/Z/J/L 七种方块放入一个 Bag。
2. 随机打乱。
3. 按顺序生成。
4. 七个用完后重新生成一个随机 Bag。

不要使用简单的完全随机。

这样可以避免长时间不出现某一种方块。

---

# 6. Next Queue

界面显示：

```text
NEXT
```

至少展示未来 **3 个 Tetromino**。

推荐显示 3～5 个。

要求：

- Next Queue 与实际生成顺序必须完全一致。
- 使用缩小版 Tetromino 绘制。
- 每个预览框尺寸统一。
- 不要让预览占用过多视觉空间。

---

# 7. Hold 系统

界面显示：

```text
HOLD
```

默认键位：

```text
C
```

规则：

- Hold 为空：当前方块进入 Hold，生成 Next 方块。
- Hold 非空：当前方块与 Hold 中的方块交换。
- 一个 Tetromino 在落地之前只能使用一次 Hold。
- 当前 Tetromino 锁定后重新允许 Hold。
- 交换出来的 Tetromino 应恢复为默认出生旋转状态。

禁止出现无限连续 Hold。

---

# 8. Ghost Piece

必须实现 Ghost Piece。

它表示：

> 当前 Tetromino 如果执行 Hard Drop，会落在哪里。

视觉要求：

- 使用当前 Tetromino 对应颜色。
- 透明度低。
- 可以使用淡色填充或边框。
- 不能比当前真实方块更亮。

Ghost Piece 必须使用真实碰撞检测计算，不能只是估算。

Ghost 位置必须与 Hard Drop 最终位置完全一致。

---

# 9. 基础操作

桌面键盘：

| 按键 | 操作 |
|---|---|
| ← | 左移 |
| → | 右移 |
| ↓ | Soft Drop |
| ↑ | 顺时针旋转 |
| Z | 逆时针旋转 |
| Space | Hard Drop |
| C | Hold |
| P | Pause / Resume |
| Esc | Pause |

Hard Drop：

- 立即下降到底部。
- 立即锁定。
- Hard Drop 后禁止继续移动或旋转。

Soft Drop：

- 加速下降。
- 可给予少量额外分数。

---

# 10. 键盘手感：DAS / ARR

为了让左右移动手感自然，实现简化版 DAS / ARR。

### DAS

玩家按住 ← 或 → 时：

- 第一次立即移动 1 格。
- 短暂延迟后开始连续移动。

建议：

```text
DAS ≈ 140～170ms
```

### ARR

连续移动间隔建议：

```text
ARR ≈ 35～55ms
```

目标：

- 短按可以精确移动 1 格。
- 长按可以快速移动到左右侧。
- 不会因为浏览器 Keyboard Repeat 差异导致手感不一致。

请自行维护按键状态，不要完全依赖浏览器默认 repeat。

---

# 11. Lock Delay

必须加入轻量 **Lock Delay（落地延迟）**。

当 Tetromino 接触地面或已有方块后：

不要立刻锁定。

给玩家大约：

```text
400～500ms
```

用于：

- 左右移动
- 旋转
- 微调位置

玩家在 Lock Delay 内进行有效移动或旋转后，可适度刷新锁定时间。

但要设置最大刷新次数，例如：

```text
最多 10～15 次
```

避免无限拖延。

Hard Drop 不受 Lock Delay 影响，直接锁定。

---

# 12. 旋转与 Wall Kick

实现顺时针和逆时针旋转。

不强制完整官方 SRS，但至少实现合理 Wall Kick。

旋转失败时依次尝试附近偏移：

```text
0
左 1
右 1
左 2
右 2
上 1
```

I Piece 可以使用稍微不同的偏移规则。

目标：

- 靠墙时仍有合理旋转机会。
- 不允许旋转穿进墙里。
- 不允许旋转穿入已有方块。

---

# 13. T-Spin

Classic 和 Chill 都支持基础 T-Spin 检测。

至少识别：

- T-SPIN
- T-SPIN SINGLE
- T-SPIN DOUBLE
- T-SPIN TRIPLE（若逻辑允许）

无需追求职业比赛级别复杂判定。

可采用简化规则：

- 当前方块为 T。
- 最后一个有效操作是旋转。
- T 中心周围四个角中至少三个被墙体或方块占据。

触发后：

- 有额外分数。
- 显示短暂文字提示。
- 可参与 Back-to-Back。

---

# 14. 消行

完整支持：

- Single
- Double
- Triple
- Tetris（4 行）

推荐动画：

1. 完整行快速变亮。
2. 150～300ms 内消散。
3. 可从中心向两侧消失。
4. 同时生成少量像素粒子。
5. 上方方块下落填补。

动画不能拖慢游戏逻辑太久。

不要因为动画导致重复消行。

---

# 15. 计分

UI 显示：

- SCORE
- BEST
- LEVEL
- LINES
- COMBO

基础计分建议：

```text
Single   = 100 × Level
Double   = 300 × Level
Triple   = 500 × Level
Tetris   = 800 × Level
```

额外分数：

### Soft Drop

```text
每格 +1
```

### Hard Drop

```text
每格 +2
```

### Combo

连续消行时获得额外分数。

### Back-to-Back

连续完成以下高价值动作：

- Tetris
- T-Spin 消行

给予额外倍率。

推荐：

```text
×1.5
```

---

# 16. Combo

规则：

- 当前 Tetromino 消行 → Combo 增加。
- 下一 Tetromino 继续消行 → Combo 再增加。
- 某个 Tetromino 未消行 → Combo 重置。

显示示例：

```text
COMBO x2
COMBO x3
COMBO x4
```

动画：

- 小幅上浮。
- 淡出。
- 不遮挡棋盘核心区域。

高 Combo 可出现：

```text
NICE!
GREAT!
AWESOME!
```

不要频繁弹字。

---

# 17. Back-to-Back

连续高价值消除时显示：

```text
BACK TO BACK
```

可以给予：

- 较明显音效
- 短暂文字动画
- 额外得分

视觉效果比普通消行强，但比 Game Over、Emergency Clear 克制。

---

# 18. 等级与速度

## Classic Mode

建议：

```text
每消除 10 行提升 1 级
```

随着等级提升：

- Gravity 增加。
- 下落速度逐步加快。

速度曲线必须平滑。

禁止出现：

> Level 1 非常慢 → Level 2 突然快一倍。

可以使用分段或指数函数，但要限制最高速度。

---

## Chill Mode

升级规则可以相同，但 Gravity 增长更慢。

建议：

- 初始下降更慢。
- 前 3～5 级变化很轻。
- 后期仍增加压力，但不要瞬间变成竞技难度。

目标：

> 普通玩家可以比较放松地玩 5～10 分钟。

---

# 19. Chill Mode 道具系统

道具不要太频繁。

建议平均：

```text
每 15～25 个正常 Tetromino 出现一次特殊奖励/事件
```

采用随机冷却机制，避免连续多个道具。

至少实现以下 3 种。

---

## 19.1 BOMB BLOCK

特殊炸弹方块。

表现：

- 带明显炸弹图标。
- 与普通 Tetromino 有视觉区别。

落地后：

- 清除以炸弹核心为中心的小范围区域。
- 推荐范围 3×3。

限制：

- 不允许爆炸清除游戏边界之外的数据。
- 爆炸后正常进行重力/消行判断。

动画：

- 短暂白闪。
- 像素粒子向外散射。
- 时长控制在 300ms 左右。

---

## 19.2 LINE CLEAR ITEM

触发后自动清除：

```text
棋盘最底部的非空行
```

如果最底部为空：

从下向上寻找第一条非空行并清除。

如果整个棋盘为空：

不执行清除，并给予简短提示。

---

## 19.3 SLOW TIME

触发后：

```text
约 10 秒内 Gravity 明显降低
```

表现：

- UI 显示 `SLOW TIME`。
- 显示简短倒计时。
- 棋盘边缘出现轻微冷色调滤镜。

暂停游戏时：

Slow Time 倒计时也必须暂停。

---

# 20. Chill Mode 续命

Chill Mode 每局有一次续命。

第一次触顶时：

不要直接 Game Over。

触发：

```text
EMERGENCY CLEAR!
```

推荐流程：

1. 游戏冻结约 400～600ms。
2. CRT 屏幕短暂闪烁。
3. 中央显示 `EMERGENCY CLEAR!`。
4. 清理棋盘危险区域。
5. 播放特殊音效。
6. 恢复游戏。

清理方案优先推荐：

> 找到堆积最高区域，自上而下移除约 30%～40% 的已占用格子，并保证结果不会留下完全悬空、难以理解的大量碎片。

更稳定的实现方式也可以是：

> 删除顶部若干非空行，再将剩余方块整体下落。

建议后者，更容易保证规则稳定。

每局只能触发一次。

触发后 UI 显示：

```text
REVIVE USED
```

第二次触顶：

正常 Game Over。

Classic Mode 不存在该机制。

---

# 21. 开始界面

网页加载后不要立刻开始掉方块。

先显示街机风主菜单。

标题：

```text
RETRO BLOCKS
```

副标题：

```text
TAKE A BREAK.
CLEAR YOUR MIND.
CLEAR SOME LINES.
```

或：

```text
INSERT COIN
PRESS START
```

主按钮：

```text
CLASSIC MODE
CHILL MODE
```

辅助按钮：

```text
HOW TO PLAY
SETTINGS
```

主界面可显示：

```text
CLASSIC BEST
CHILL BEST
```

避免菜单过于复杂。

---

# 22. 开场倒计时

开始一局时：

显示：

```text
READY
3
2
1
GO!
```

倒计时总长度控制在 2～3 秒。

玩家点击 Restart 时也可以使用更短倒计时。

---

# 23. Pause

按：

```text
P
Esc
```

暂停。

暂停时显示半透明遮罩：

```text
PAUSED
PRESS P TO CONTINUE
```

暂停期间必须：

- Gravity 停止。
- 输入移动无效。
- Lock Delay 停止。
- Slow Time 停止计时。
- 粒子动画可以冻结或轻微继续，但不要影响逻辑。

---

# 24. 自动暂停

监听：

```js
document.visibilitychange
```

当：

```js
document.hidden === true
```

自动 Pause。

返回页面后：

**不要自动继续。**

必须由玩家主动按 P、Esc 或点击 Resume。

这样用户切去看论文、聊天或网页后，不会回来直接 Game Over。

---

# 25. Game Over

游戏结束时不要刷新页面。

显示结算层：

```text
GAME OVER

SCORE
LINES
LEVEL
BEST
```

如果创造最高分：

```text
NEW HIGH SCORE!
```

按钮：

```text
PLAY AGAIN
MAIN MENU
```

Game Over 后：

- 键盘不再控制棋盘。
- 游戏循环逻辑保持安全状态。
- 音频可以切换到简短结算音效。

---

# 26. 本地最高分与设置

使用：

```js
localStorage
```

保存：

- Classic High Score
- Chill High Score
- Music Volume
- SFX Volume
- Music On/Off
- SFX On/Off
- Reduced Motion
- Color Assist

所有 localStorage 操作都必须包裹异常处理。

如果浏览器禁止 localStorage：

游戏仍然正常运行，只是不持久化。

---

# 27. Settings 设置面板

设置不要复杂，但建议提供：

### AUDIO

- Music On / Off
- Music Volume 0～100%
- SFX On / Off
- SFX Volume 0～100%

默认：

```text
Music = 30%
SFX = 50%
```

### DISPLAY

- CRT Effect On / Off
- Reduced Motion On / Off
- Color Assist On / Off

### CONTROLS

展示当前键位。

不强制做完整键位自定义。

如果实现键位修改，必须保证不会出现两个关键操作绑定同一个键而无法操作的问题。

---

# 28. 色弱友好模式

加入可选：

```text
COLOR ASSIST
```

开启后：

除了颜色以外，每种 Tetromino 增加独立的小型纹理或标记。

例如：

- I：横线纹
- O：点阵
- T：十字纹
- S：斜线纹
- Z：反斜线纹
- J：双点
- L：方格纹

不要改变 Tetromino 本身形状。

这样颜色辨识困难的玩家也可以区分。

---

# 29. Reduced Motion

提供：

```text
REDUCED MOTION
```

开启后：

- 禁止屏幕震动。
- 减少粒子。
- 消行只做简单淡出。
- Tetris 不做强烈闪烁。
- 菜单转场缩短。

同时检测：

```css
prefers-reduced-motion
```

如果系统设置偏好减少动态效果，应默认尊重。

---

# 30. 视觉风格

视觉方向：

```text
Retro Arcade + Neon + CRT
```

主背景不要纯黑。

推荐：

- 深蓝
- 深紫
- 墨绿色
- 暗灰蓝

主棋盘：

- 比背景略深。
- 有复古显示器边框。
- 边缘微弱 Glow。

UI 面板：

- 像街机仪表。
- 数字可使用七段显示器/像素风效果。

不要用过度现代的 SaaS 风格卡片。

---

# 31. CRT 效果

可加入：

- 扫描线
- 微弱噪点
- 暗角
- 极轻微 RGB 色散
- 微弱 Bloom / Glow

必须克制。

优先保证：

> 棋盘清晰 > CRT 氛围。

禁止：

- 强烈抖屏
- 高频闪烁
- 大范围炫光遮挡

---

# 32. 动画

## 方块落地

可加入：

- 轻微 squash
- 很短的亮度变化
- 1～2px 的局部震动感

不要让整个页面大幅震动。

## 消行

完整行：

- 快速变亮
- 像素碎裂
- 150～300ms 内消失

## Tetris

显示：

```text
TETRIS!
```

伴随：

- 更明显的光效
- 特殊音效
- 少量额外粒子

## T-Spin

显示：

```text
T-SPIN!
```

---

# 33. 粒子系统

使用 Canvas 内部粒子系统。

消行时产生：

```text
约 10～30 个粒子
```

每个粒子包含：

- x
- y
- vx
- vy
- life
- maxLife
- size
- color

每帧：

- 更新位置
- 衰减 life
- 移除 life <= 0 的粒子

设置最大粒子数，例如：

```text
MAX_PARTICLES = 150
```

避免长时间游戏后数组无限增长。

---

# 34. 音效

使用 Web Audio API。

禁止依赖外部 mp3。

至少包含：

- Move
- Rotate
- Soft Drop
- Hard Drop
- Lock
- Single Clear
- Double Clear
- Triple Clear
- Tetris
- T-Spin
- Combo
- Back-to-Back
- Hold
- Pause
- Level Up
- Bomb
- Slow Time
- Emergency Clear
- Game Over

风格：

```text
8-bit / chiptune / arcade
```

要求：

- 音效短。
- 不刺耳。
- 不要每移动一次就过于响亮。

Move 音效应非常轻。

---

# 35. 背景音乐

使用 Web Audio API 自行合成轻量 Chiptune BGM。

可组合：

- square oscillator
- triangle oscillator
- sine oscillator

音乐特点：

- 轻松
- 节奏适中
- 不紧张
- 不随着等级疯狂加速
- 可长时间低音量循环

Classic 与 Chill 可以使用同一主题的不同编曲：

- Classic 稍有节奏感。
- Chill 更柔和。

AudioContext 必须在用户第一次点击 Start 后创建/恢复。

不要在页面加载时自动播放。

---

# 36. 移动端

必须 Responsive。

手机屏幕不能出现页面横向滚动。

移动端布局建议：

```text
顶部：Score / Level / Lines
中间：棋盘
棋盘两侧或顶部：Hold / Next
底部：触控按钮
```

触控按钮：

```text
←
→
↓
ROTATE
DROP
HOLD
```

按钮尺寸至少适合手指点击。

建议使用 Pointer Events。

桌面宽屏下：

- 可以隐藏触控按钮。
- 或降低其视觉权重。

---

# 37. 桌面布局

推荐：

```text
┌───────────┬───────────────┬──────────────┐
│   HOLD    │               │     NEXT     │
│           │               │              │
│           │    BOARD      │    SCORE     │
│           │               │    BEST      │
│           │               │    LEVEL     │
│           │               │    LINES     │
│           │               │    COMBO     │
└───────────┴───────────────┴──────────────┘
```

下方显示：

```text
← → Move   ↑ Rotate   ↓ Soft Drop   Space Drop   C Hold   P Pause
```

---

# 38. HOW TO PLAY

主菜单提供轻量帮助页面。

内容包含：

- 基础目标
- 键位
- Hold
- Ghost
- Combo
- Chill Mode 道具

不要写成大段说明书。

推荐图标 + 一句话。

---

# 39. 休息体验细节

可以加入一些很轻量的友好提示。

例如：

Chill Mode 连续玩满约 10 分钟：

```text
GOOD BREAK.
READY TO GO BACK?
```

只做提示。

不要：

- 强制暂停
- 强制结束
- 倒计时赶玩家离开

如果玩家继续玩，游戏正常继续。

---

# 40. 彩蛋

可以加入少量彩蛋。

### 连续 3 次 Tetris

显示：

```text
BLOCK MASTER
```

### 高 Combo

例如 Combo ≥ 8：

```text
ON FIRE!
```

### Pause 一段时间

显示：

```text
TAKE YOUR TIME.
```

彩蛋只能是视觉/文字反馈，不影响核心规则。

---

# 41. 程序结构

JavaScript 逻辑至少清晰拆分为以下模块/对象：

```text
Game
Board
Piece
Bag / Queue
Input
AudioManager
ParticleSystem
UI
Settings
Storage
```

可以使用 ES6 class，也可以使用模块化对象。

要求：

- 避免几十个无组织的全局变量。
- 游戏状态集中管理。
- Restart 可以彻底重置状态。
- Main Menu 可以安全销毁本局状态。

推荐状态：

```js
MENU
COUNTDOWN
PLAYING
PAUSED
CLEARING
GAME_OVER
```

所有输入和更新根据 state 判断。

---

# 42. 时间系统

游戏逻辑不要直接依赖固定帧率。

使用：

```js
performance.now()
```

计算 Delta Time。

需要处理：

- 浏览器卡顿
- 标签页切换
- 恢复页面后的超大 delta

建议对 delta 做 clamp，例如最大：

```text
50～100ms
```

切换标签页时直接 Pause，避免恢复后一次性补掉大量 Gravity。

---

# 43. 关键 Bug 防护

必须主动检查以下问题：

1. 快速左右移动导致穿墙。
2. Tetromino 穿过已有方块。
3. 旋转后进入墙体。
4. Wall Kick 越界。
5. Hard Drop 后还能继续移动。
6. Hard Drop 后重复 Lock。
7. Hold 可以在同一个方块生命周期内连续使用。
8. Hold 后 Next Queue 错位。
9. Next Queue 显示与实际生成顺序不同。
10. Ghost Piece 和真实 Hard Drop 落点不一致。
11. Pause 后 Gravity 仍运行。
12. Pause 后 Lock Delay 仍计时。
13. Pause 后 Slow Time 仍倒计时。
14. 浏览器切后台后回来瞬间掉很多格。
15. Clear Animation 期间重复触发行清除。
16. Restart 后旧的 requestAnimationFrame 逻辑仍修改新游戏。
17. Restart 后旧 timeout / interval 仍在执行。
18. Game Over 后键盘仍控制棋盘。
19. Emergency Clear 后当前 Piece 与 Board 发生冲突。
20. Bomb 清除边缘区域导致数组越界。
21. localStorage 抛错导致游戏崩溃。
22. AudioContext 未经过用户交互就启动。
23. 手机屏幕出现横向溢出。
24. 长时间运行后粒子数组无限增长。
25. 连续旋转可以无限刷新 Lock Delay。
26. Soft Drop 分数被重复计算。
27. T-Spin 被普通 T 方块误判。
28. 页面 Resize 后 Canvas 模糊或比例错误。
29. 高 DPI 屏幕 Canvas 严重模糊。
30. 触控按钮出现长按网页选中文字。

---

# 44. Canvas 与高 DPI

Canvas 必须兼顾 Retina / 高 DPI 显示器。

使用：

```js
window.devicePixelRatio
```

设置内部像素尺寸，同时保持 CSS 尺寸一致。

确保：

- 方块边缘清晰。
- Resize 后重新计算。
- 不改变棋盘逻辑坐标。

---

# 45. 性能要求

目标：

```text
稳定接近 60FPS
```

优化原则：

- 一个主要 requestAnimationFrame 循环。
- 尽量避免多个并行 RAF。
- 不创建无限 Timer。
- 粒子有数量上限。
- 不在每帧大量创建临时对象。
- 不每帧操作大量 DOM。

UI 数字仅在值发生变化时更新。

---

# 46. UI 文案

主要使用英文街机风：

```text
RETRO BLOCKS
CLASSIC MODE
CHILL MODE
SCORE
BEST
LEVEL
LINES
NEXT
HOLD
PAUSED
GAME OVER
TETRIS!
T-SPIN!
COMBO
BACK TO BACK
LEVEL UP
SLOW TIME
EMERGENCY CLEAR
REVIVE USED
NEW HIGH SCORE!
```

中文主要用于：

- 设置说明
- 操作帮助
- 音量说明

---

# 47. 推荐游戏常量

所有核心参数集中放在一个 Config 对象中，方便调试。

例如：

```js
const CONFIG = {
  COLS: 10,
  ROWS: 20,
  PREVIEW_COUNT: 3,

  LOCK_DELAY: 450,
  MAX_LOCK_RESETS: 12,

  DAS: 160,
  ARR: 45,

  CHILL_ITEM_MIN: 15,
  CHILL_ITEM_MAX: 25,
  SLOW_TIME_DURATION: 10000,

  MAX_PARTICLES: 150,

  DEFAULT_MUSIC_VOLUME: 0.30,
  DEFAULT_SFX_VOLUME: 0.50
};
```

不要把这些数字散落在代码各处。

---

# 48. 开发优先级

开发时按以下顺序确保稳定：

## P0 —— 必须正确

1. Board
2. Piece
3. Collision
4. Rotation
5. Lock
6. Line Clear
7. 7-Bag
8. Next
9. Hold
10. Ghost
11. Pause
12. Restart
13. Game Over

## P1 —— 完整体验

14. Score
15. Level
16. Combo
17. Back-to-Back
18. Lock Delay
19. DAS / ARR
20. T-Spin
21. localStorage
22. Mobile Controls

## P2 —— Chill Mode

23. Bomb
24. Line Clear Item
25. Slow Time
26. Emergency Clear

## P3 —— 打磨

27. Audio
28. BGM
29. Particle
30. CRT
31. Reduced Motion
32. Color Assist
33. Easter Eggs

如果 P0 有 Bug，不要为了动画继续堆功能。

---

# 49. 验收测试

生成最终代码前，至少自行模拟并检查以下场景。

## 基础

- [ ] 游戏可以进入 Classic。
- [ ] 游戏可以进入 Chill。
- [ ] 左右移动正常。
- [ ] Soft Drop 正常。
- [ ] Hard Drop 正常。
- [ ] 顺时针旋转正常。
- [ ] 逆时针旋转正常。
- [ ] Tetromino 不会穿墙。
- [ ] Tetromino 不会穿过已有方块。

## Queue / Hold

- [ ] 7-Bag 工作正常。
- [ ] Next Queue 顺序正确。
- [ ] Hold 第一次正常。
- [ ] 同一个 Piece 不能连续 Hold 两次。
- [ ] Piece Lock 后可以再次 Hold。

## Ghost

- [ ] Ghost 不与真实 Piece 混淆。
- [ ] Ghost 位置等于 Hard Drop 最终位置。

## 消行

- [ ] Single 正常。
- [ ] Double 正常。
- [ ] Triple 正常。
- [ ] Tetris 正常。
- [ ] 多行消除后上方 Board 正确下移。

## 特殊得分

- [ ] Combo 正常增加和重置。
- [ ] Back-to-Back 正常。
- [ ] T-Spin 不会明显误判。

## Pause

- [ ] P 可暂停。
- [ ] Esc 可暂停。
- [ ] Pause 时 Gravity 停止。
- [ ] 页面切后台自动暂停。
- [ ] 回到页面不会自动继续。

## Game Over

- [ ] Classic 触顶 Game Over。
- [ ] Chill 第一次触顶触发 Emergency Clear。
- [ ] Chill 第二次触顶 Game Over。
- [ ] Game Over 后按键不会移动 Piece。

## Chill 道具

- [ ] Bomb 正常。
- [ ] Bomb 在边缘不会越界。
- [ ] Line Clear Item 正常。
- [ ] Slow Time 正常。
- [ ] Pause 后 Slow Time 停止计时。

## Restart

- [ ] Restart 后棋盘清空。
- [ ] Score 清零。
- [ ] Queue 重新生成。
- [ ] Hold 清空。
- [ ] Combo 清空。
- [ ] Chill Revive 恢复。
- [ ] 不存在旧 Timer 干扰新局。

## Storage

- [ ] Classic Best 独立保存。
- [ ] Chill Best 独立保存。
- [ ] 设置保存。
- [ ] localStorage 失败时不崩溃。

## Audio

- [ ] 点击 Start 后才能创建/恢复音频。
- [ ] Music 音量有效。
- [ ] SFX 音量有效。
- [ ] Mute 有效。

## Mobile

- [ ] 320px 宽屏幕不横向溢出。
- [ ] 左右按钮可用。
- [ ] Rotate 可用。
- [ ] Drop 可用。
- [ ] Hold 可用。
- [ ] 不会因长按按钮选择网页文本。

---

# 50. 最终交付要求

最终必须输出一个完整：

```text
index.html
```

不得：

- 省略函数。
- 使用伪代码。
- 写 TODO。
- 写“其他部分类似”。
- 只给核心代码片段。
- 只做视觉 Demo。
- 只做不能运行的概念页面。

所有按钮必须真正可用。

所有模式必须真正可玩。

所有核心功能必须相互兼容。

---

# 51. 给 Codex 的最终执行指令

请严格按照本开发文档实现项目。

实现前先规划代码结构，但不要在最终回答中输出规划过程。

优先保证俄罗斯方块核心规则、状态管理和输入逻辑正确，再完成视觉与音效。

请在最终生成代码前主动检查：

- Board
- Collision
- Rotation
- Wall Kick
- Lock Delay
- 7-Bag
- Next Queue
- Hold
- Ghost Piece
- Hard Drop
- Line Clear
- Score
- Combo
- Back-to-Back
- T-Spin
- Level
- Pause
- Visibility Pause
- Restart
- Game Over
- Chill Items
- Emergency Clear
- localStorage
- AudioContext
- Mobile Controls
- Responsive Layout
- High DPI Canvas

最终回复不要解释代码实现思路。

不要先输出设计说明。

直接输出完整、可运行的：

```html
index.html
```

并确保复制保存后可以直接双击运行。
