# 俄罗斯方块开发文档 v2.2 — 增量更新说明

> 本文档仅记录相对于原始开发文档及 v1.1～v2.1 的新增、修改与调整内容。  
> 未在本文档中提及的功能、规则、视觉设计与技术要求，继续沿用之前版本。  
> 若本文件与旧版本存在冲突，以 v2.2 为准。

---

# 1. 统一 Touch / Mouse 点击操作

当前问题：

- 触摸双击与鼠标双击行为不一致
- 双端操作规则不统一，增加学习成本

v2.2 正式统一为：

- **单击 / 单触：顺时针旋转**
- **双击 / 双触：Hard Drop / 直接落下**
- **按住并左右拖动：实时横向移动**

Touch 与 Mouse 统一遵守这一规则。

---

# 2. 点击与拖动必须互斥

单击、双击、拖动不能互相串动作。

要求：

- 一旦判定为有效拖动，该 Pointer 序列不能再触发单击旋转
- 一旦进入 Drag，也不能参与双击判定
- Drag 结束时不能因为 pointerup 再补一次旋转
- 双击不能先执行旋转再执行 Hard Drop

---

# 3. 单击 / 双击判定机制

不要依赖浏览器默认 `click / dblclick` 作为核心逻辑。

推荐使用自定义 Pointer 状态机：

```text
pointerdown
→ 记录时间、位置
→ 标记潜在 Tap

pointermove
→ 若超过 Drag Threshold
→ 进入 Drag
→ 取消 Tap 候选
→ 实时横向移动

pointerup
→ 若未进入 Drag
→ 记为一次 Tap
```

---

# 4. 单击旋转需要短暂判定窗口

第一次 Tap 后：

- 等待约 **180～220ms**
- 若窗口内没有第二次 Tap：
  - 执行一次顺时针旋转
- 若窗口内出现第二次有效 Tap：
  - 取消待执行旋转
  - 立即 Hard Drop

推荐默认：

**约 200ms**

---

# 5. 第一次 Tap 的即时视觉反馈

为了避免 200ms 判定窗口显得迟钝：

第一次 Tap 时可以立即给轻量视觉反馈，例如：

- 当前活动方块边缘轻微亮一下
- 极短输入反馈

但不要立即改变实际方块状态。

若确认是单击：
- 再真正旋转

若确认是双击：
- 取消待旋转
- 立即 Hard Drop

---

# 6. 双击判定条件

两次 Tap 必须满足：

- 时间间隔小于设定阈值
- 两次位置足够接近
- 两次都没有进入 Drag
- 都发生在有效 Gesture Zone 内

建议位置差：

- 约 **24～32px** 内

具体数值允许按设备 DPI 微调。

---

# 7. 双击必须只执行 Hard Drop

第二次 Tap 被识别为双击时：

- 立即取消第一击待执行的旋转
- 立即执行 Hard Drop
- 不额外旋转

最终行为：

**双击 = 只 Hard Drop**

禁止：

`Rotate + Hard Drop`

---

# 8. 拖动继续采用实时 Pointer Move

沿用 v2.1：

- pointermove 中实时执行
- 不等 pointerup
- 每超过一个格距阈值移动一格
- 支持实时反向
- 碰墙后 Gesture 不失效
- 使用 Pointer Capture
- 始终逐格碰撞检测

---

# 9. Tap / Drag 状态机建议

建议至少包含：

- IDLE
- TAP_PENDING
- DRAGGING
- DOUBLE_TAP
- CANCELLED

避免多个 timeout 与 if 逻辑互相打架。

---

# 10. Pointer 状态清理

以下情况都必须清理输入状态：

- pointerup
- pointercancel
- lostpointercapture
- 页面失焦
- Pause
- Game Over

必须：

- 清理待执行 Tap Timer
- 清理 Drag 状态
- 释放 Pointer Capture
- 防止旧输入延迟到下一块执行

---

# 11. 教程期间仍保持当前 3 个按钮

v2.2 明确：

**Tutorial 期间不得把底部控制区扩展成 6 个按钮。**

始终保持当前现有的：

**3 个主要触控按钮**

教程只作为：

- Overlay
- Gesture Hint
- 视觉提示层

不得改变按钮数量。

---

# 12. 教程不得触发布局重排

进入 Gesture Tutorial 时：

- 不增加按钮
- 不扩大控制区高度
- 不重新计算成 6 按钮布局
- 不因为 Tutorial 导致棋盘缩小

---

# 13. Gesture Tutorial 默认始终显示

只要：

**Gesture Control = ON**

则每次新开始一局游戏：

**默认播放手势教学。**

不再依赖：

- 首次运行
- gestureTutorialCompleted

---

# 14. Tutorial 顺序更新

每次新局建议依次提示：

1. 左滑移动
2. 右滑移动
3. 单击旋转
4. 双击落下

---

# 15. 左右滑教程箭头沿用 v2.1

左滑 / 右滑：

必须使用：

- 单个
- 完整
- 大型
- 半透明浅蓝色
- 有箭身
- 有三角箭头

的图形箭头。

禁止：

- emoji
- 文本箭头字符
- 多箭头排列
- `>>>`
- 手指 emoji

推荐：

- Inline SVG

---

# 16. 单击旋转教学

新增 / 更新为：

**单击旋转**

建议显示：

- 当前已有点击视觉
- 轻量顺时针旋转标记
- 中文：`单击旋转`

完成条件：

玩家完成一次有效单击旋转：

- 提示立即消失
- 进入下一步

---

# 17. 双击落下教学

新增：

**双击落下**

提示：

- 双击视觉
- Hard Drop 方向提示
- 中文：`双击落下`

完成条件：

玩家完成一次有效双击：

- Hard Drop 立即执行
- 教学提示消失
- Tutorial 完成

---

# 18. 教程保持非阻塞

Tutorial 期间：

- 游戏继续
- 可移动
- 可 Hold
- 可 Pause
- 可使用按钮
- 可执行其他操作

当前步骤：

- 做出目标动作 → 立即进入下一步
- 不做 → 等待约 6～10 秒自动淡出并继续

---

# 19. Gesture Tutorial 设置

Settings 新增：

**手势教学**

选项：

- 始终显示
- 关闭

默认：

**始终显示**

如果设为关闭：

- Gesture Control 仍正常工作
- 仅不再显示教学

---

# 20. Gesture Control 与 Tutorial 分离

两者是独立设置：

```text
Gesture Control：ON
Gesture Tutorial：OFF
```

此时：

- 手势可正常使用
- 不显示教学

---

# 21. Tutorial 必须与当前真实操作一致

v2.2 当前必须教学：

- 左滑 → 左移
- 右滑 → 右移
- 单击 → 顺时针旋转
- 双击 → Hard Drop

不得继续出现：

- 双击旋转

---

# 22. Touch / Mouse 使用同一套空间操作语义

Touch 与 Mouse 统一：

## Tap / Click
- 顺时针旋转

## Double Tap / Double Click
- Hard Drop

## Drag
- 实时横向移动

---

# 23. 键盘操作保持不变

键盘继续：

- ← / →：左右移动
- ↓：Soft Drop
- ↑：顺时针旋转
- Space：Hard Drop
- C：Hold
- P / Esc：Pause
- U：OOPS（Chill 可用时）

手势仅作为额外 Input Method。

---

# 24. Drag 不参与 Double Tap

如果 Pointer 发生明显 Drag：

- 当前操作不算 Click
- 不参与 Double Tap

避免：

拖动结束后又误触旋转 / Hard Drop。

---

# 25. Gesture Zone 继续沿用中间主体安全区域

单击 / 双击 / 拖动可发生在：

**中间游戏主体 Gesture Zone**

不必严格限定于 10×20 棋盘框。

必须排除：

- 顶栏
- 按钮
- Pause
- Settings
- Guide
- Slider
- Recent Surprises 查看按钮
- 其他交互控件

---

# 26. 可选：Tap / Drag 状态反馈

可增加非常轻量反馈：

### Tap Pending
第一次点击：
- 方块边缘轻亮

### Dragging
进入拖动：
- 当前活动方块边缘亮度略变

### Double Tap
识别成功：
- 直接 Hard Drop
- 配合 Hard Drop 音效

反馈必须克制。

---

# 27. v2.2 验收测试

## 单击
Touch / Mouse：
- 单击约 200ms 后只执行一次顺时针旋转
- 不 Hard Drop

## 双击
Touch / Mouse：
- 双击只执行 Hard Drop
- 不先旋转

## Drag
- pointermove 中实时横移
- pointerup 不补旋转
- Drag 不参与 Tap / Double Tap

## 双击位置
两次点击位置差过大：
- 不判定为双击

## Tutorial 按钮
- Tutorial 开启时仍保持当前 3 个按钮
- 不出现 6 按钮布局
- 棋盘尺寸不因 Tutorial 变化

## Tutorial 默认行为
Gesture Control = ON：
- 每次新局默认显示 Tutorial

Gesture Tutorial = OFF：
- 手势可用
- 不显示 Tutorial

## Tutorial 步骤
必须为：
1. 左滑
2. 右滑
3. 单击旋转
4. 双击落下

不得再出现“双击旋转”。

## Tutorial 非阻塞
- 教学期间可正常游戏
- 可 Hold / Pause / 按按钮
- 不做目标动作时约 6～10 秒自动淡出

---

# 28. Windows EXE 打包预留

游戏继续以：

- HTML
- CSS
- JavaScript

为核心。

推荐未来优先：

**Tauri 2**

原因：

- 可直接复用现有前端
- Windows 使用系统 WebView2
- 相比 Electron 更轻量
- 可生成 Windows 安装包
- 同一前端未来可继续复用到 Android

---

# 29. 推荐 Tauri 项目结构

```text
retro-blocks/
├─ index.html
├─ package.json
└─ src-tauri/
   ├─ tauri.conf.json
   ├─ Cargo.toml
   └─ icons/
```

Windows 目标：

- 独立 `.exe`
- 安装程序
- 独立任务栏图标
- 独立桌面图标
- 无浏览器地址栏

---

# 30. Electron 作为 Windows 备选

如果优先目标是：

- 最快封装
- 不介意更大的安装包

可以使用：

**Electron**

优点：

- Web 生态成熟
- HTML 基本无需重写

缺点：

- 通常更大
- 自带 Chromium
- 对轻量小游戏偏重

---

# 31. Android APK 打包预留

推荐路线：

## 方案 A：Tauri 2

优先推荐。

优势：

- 与 Windows 共用同一前端
- 减少多套项目维护
- 可继续构建 Android 应用

## 方案 B：Capacitor

如果只想快速包装为 Android：

- 复用现有 Web 前端
- 创建 Android Project
- 使用 Android Studio 构建 APK

缺点：

若 Windows 使用 Tauri，则会形成两套壳技术。

---

# 32. 推荐项目演进路线

## 阶段 1

继续维护：

**单 HTML / Web 版**

直到：

- 操作稳定
- Chill 稳定
- 手机布局稳定
- Gesture 稳定

## 阶段 2

转为：

**Tauri 2**

尽量使用同一份前端生成：

- Windows EXE
- Android APK

---

# 33. 打包后不得破坏的现有能力

桌面 / Android 打包后必须继续支持：

- Keyboard
- Mouse
- Touch
- Gesture Control
- 单击旋转
- 双击 Hard Drop
- 实时 Drag
- BGM
- SFX
- localStorage / 设置保存
- Focus Mode
- Classic / Chill / Zen
- Tutorial
- Surprise History

---

# 34. 平台化后可增加的能力

未来可加入：

## Windows
- 自定义 `.ico`
- 独立窗口
- 最小窗口尺寸
- 任务栏图标
- 桌面快捷方式

## Android
- 自定义 App Icon
- 启动画面
- 状态栏控制
- 屏幕方向策略
- 沉浸式全屏

这些属于应用封装阶段，不要求当前 HTML 立即实现。

---

# 35. v2.2 核心总结

## 第一：统一单双击
- 单击 / 单触 = 顺时针旋转
- 双击 / 双触 = Hard Drop
- Touch / Mouse 一致

## 第二：正确区分 Tap / Double Tap / Drag
- 自定义 Pointer 状态机
- 约 200ms 判定窗口
- Double Tap 取消待 Rotate
- Drag 与 Tap 完全互斥

## 第三：Tutorial 不再改变按钮布局
- 始终保持当前 3 个按钮
- Tutorial 只是 Overlay
- 每局默认显示
- 可在 Settings 关闭
- 教学顺序：
  - 左滑
  - 右滑
  - 单击旋转
  - 双击落下

## 第四：准备应用化
- 推荐 Tauri 2
- Windows EXE
- Android APK
- Electron / Capacitor 作为备选
- 尽量保持单一前端代码库

开发实现时：

**以原开发文档 + v1.1～v2.1 为基础，并以本 v2.2 覆盖所有与单击 / 双击判定、Touch / Mouse 操作统一、Gesture Tutorial 默认行为、Tutorial 按钮布局以及 EXE / APK 打包预留相关的旧要求。**
