# 俄罗斯方块开发文档 v2.0 — 增量更新说明

> 本文档仅记录相对于原始开发文档及 v1.1～v1.9 的新增、修改与调整内容。  
> 未在本文档中提及的功能、规则、视觉设计与技术要求，继续沿用之前版本。  
> 若本文件与旧版本存在冲突，以 v2.0 为准。

---

# 1. v2.0 核心目标

v2.0 重点解决两个问题：

1. **窄屏 / 窄高屏布局仍不够稳健**
2. **手机端增加可选手势控制，并提供真正的交互式手势教学**

本版本不新增复杂玩法系统，重点提升：

- 移动端完整度
- 窄屏稳定性
- 手势操作自然度
- 新玩家上手体验
- 触控 UI 的灵活性

---

# 2. 修复窄屏顶部音量图标穿模

当前问题：

在较窄窗口或手机端：

- 左上角音量图标可能与顶部栏边界、标题或其他元素发生穿模
- 图标可能超出容器
- 点击区域可能与其他控件重叠

v2.0 要求：

**顶部音量图标必须完全纳入正常布局流，不允许依赖容易穿模的绝对定位。**

---

## 2.1 顶部栏布局要求

窄屏顶部栏建议使用：

- Flex
- Grid

而不是：

- 大量 `position: absolute`
- 负 margin
- 固定像素偏移

推荐结构：

### 左侧

- 音量
- 可选返回 / 菜单

### 中间

- 当前模式名称

### 右侧

- Pause
- Settings

---

## 2.2 音量按钮安全区

音量按钮要求：

- 不超出父容器
- 与屏幕边缘保留安全间距
- 最小触控区域约 40～44px
- 图标本身可以更小，但点击区域必须足够

若设备存在安全区域：

可兼容：

- `env(safe-area-inset-top)`
- `env(safe-area-inset-left)`
- `env(safe-area-inset-right)`

---

## 2.3 极窄屏优先级

当屏幕极窄时：

优先：

1. 缩短顶部文字
2. 隐藏次级文字
3. 使用图标
4. 压缩 gap

不得：

- 把音量按钮挤出屏幕
- 让图标与模式标题重叠
- 让按钮互相覆盖

---

# 3. 窄屏适配不能只看 Width

当前问题：

当 PC 浏览器窗口被拉成“手机宽度”，但高度很高时：

- 页面切换到了手机布局
- 游戏主体仍只占据上半部分
- 页面下方出现明显空白
- 说明当前布局只根据宽度断点判断，没有充分利用高度

v2.0 要求：

**移动 / 窄屏布局必须同时考虑 Width、Height 和 Aspect Ratio。**

---

# 4. 新增 Viewport-Aware Layout

布局系统应综合判断：

- `window.innerWidth`
- `window.innerHeight`
- `width / height`
- 实际可用游戏区域
- 顶部栏高度
- 底部触控区域高度

不能只依赖：

```css
@media (max-width: ...)
```

---

## 4.1 窄而高屏幕

例如：

- PC 窗口宽度类似手机
- 但高度 900～1200px

此时应：

- 适当放大棋盘
- 增加棋盘与控制区之间的合理间距
- 让整体内容在垂直方向更均匀
- 避免所有内容堆在顶部
- 避免底部留下大块无意义空白

---

## 4.2 窄而矮屏幕

例如：

- 360×640
- 375×667

此时优先：

1. 保证棋盘完整
2. 保证主要触控按钮可见
3. 压缩顶部栏
4. 折叠次级信息
5. 压缩间距
6. 最后才缩小棋盘

---

## 4.3 动态垂直分配

移动端布局应尽量填满可用高度。

推荐思路：

```text
可用高度
= viewport 高度
- 顶部栏
- 状态区
- 触控按钮区
- 必要安全间距
```

然后根据剩余区域动态计算棋盘尺寸。

---

# 5. 避免移动端底部大面积空白

v2.0 增加明确验收标准：

当屏幕高度明显大于内容最小高度时：

- 游戏布局应主动利用额外空间
- 不能所有组件固定贴在顶部
- 底部不应出现大块纯空白

允许：

- 棋盘放大
- 组件间距适当增加
- 触控按钮区向下自然布局
- 上下空间更均衡

但不得：

- 为了填满屏幕而无限拉大按钮
- 破坏主棋盘比例

---

# 6. 手机端新增可选手势控制

新增设置项：

**手势控制 / GESTURE CONTROL**

选项：

- OFF
- ON

默认建议：

- OFF 或首次询问
- 具体默认值可根据实际体验决定

---

# 7. 手势控制范围

v2.0 只实现以下手势：

## 7.1 左滑

作用：

- 方块向左移动

---

## 7.2 右滑

作用：

- 方块向右移动

---

## 7.3 双击

作用：

- 顺时针旋转

与 PC 保持一致：

- 只保留顺时针旋转

---

# 8. 明确不加入下滑控制

v2.0 明确：

**不实现下滑手势。**

不加入：

- 下滑 Soft Drop
- 快速下滑 Hard Drop

原因：

- 容易与左右滑动产生误判
- 会提高触屏误操作概率

因此：

Soft Drop / Hard Drop 继续通过触控按钮完成。

---

# 9. 明确不加入长按横移

v2.0 不加入：

- 长按左半边持续左移
- 长按右半边持续右移

原因：

- 增加手势复杂度
- 容易与普通点击 / 双击冲突
- 当前左滑 / 右滑已足够

---

# 10. 手势作用区域

手势识别主要限定在：

**游戏棋盘区域**

要求：

- 不在顶部菜单栏识别
- 不在 Settings / Guide 中识别
- 不在触控按钮区域识别
- 不在弹窗区域识别

目的是避免：

- 玩家点击按钮时误触手势
- 滑动菜单时移动方块

---

# 11. 左右滑动的移动逻辑

滑动距离可以映射为移动格数。

推荐：

- 短滑：1 格
- 中滑：2～3 格
- 长滑：更多格

但必须设置最大值。

要求：

- 不允许一次滑动直接穿墙
- 不允许越过已有方块
- 必须逐格进行碰撞检测
- 不能因为高速手势跳过碰撞

---

# 12. 双击旋转

双击棋盘区域：

- 顺时针旋转一次

要求：

- 只旋转一次
- 使用正常 Wall Kick
- 不穿墙
- 不穿过已锁定方块

---

## 12.1 双击判定

应设置：

- 合理双击时间阈值
- 合理最大点击间距

防止：

- 两次相隔很远的点击被误识别
- 快速拖动后被误判为双击

---

# 13. 手势与按钮可以同时存在

当 Gesture Control = ON 时：

默认仍然允许触控按钮存在。

玩家可以选择：

- 使用手势
- 使用按钮
- 混合使用

这样可以降低学习成本。

---

# 14. 触控按钮可收起

当 Gesture Control = ON 时：

新增设置：

**触控按钮显示**

建议选项：

- ALWAYS SHOW
- AUTO
- HIDE

中文显示：

- 始终显示
- 自动
- 隐藏

---

## 14.1 ALWAYS SHOW

- 手势可用
- 所有按钮继续显示

适合：

- 初次使用
- 想保留 Hard Drop / Hold 等明确按钮

---

## 14.2 AUTO

推荐默认。

行为：

- 初次进入显示完整按钮
- 完成手势教学后，可自动收起部分移动 / 旋转按钮
- Hard Drop、Hold、Pause 等核心按钮仍可保留

---

## 14.3 HIDE

适合熟悉手势的玩家。

可隐藏：

- 左移
- 右移
- 旋转

建议仍保留：

- Hard Drop
- Hold
- Pause

原因：

这些操作没有对应手势。

---

# 15. 手势教学与按钮收起联动

这是 v2.0 的重点新功能。

当玩家：

1. 在 Settings 中开启 Gesture Control
2. 开始一局游戏
3. 本设备 / 当前配置尚未完成手势教学

则：

**自动进入非阻塞式 Gesture Tutorial。**

---

# 16. 手势教学必须非阻塞

教学期间：

- 游戏继续正常运行
- 玩家可以执行任何其他动作
- 不强制暂停
- 不锁定其他按钮
- 不强制玩家必须完成教学

教学只是：

**视觉引导层。**

---

# 17. 第一步：左滑教学

进入游戏后：

在棋盘区域出现：

- 一个较大的
- 半透明
- 浅蓝色
- 带轻微闪烁 / 呼吸动画

的：

**向左滑动图标**

建议包含：

- 手指图标
- 向左箭头
- 中文：`左滑移动`
- 可选小字：`向左滑动棋盘`

---

## 17.1 左滑教学完成条件

当玩家实际完成一次有效左滑：

- 方块正常向左移动
- 左滑教学动画立即消失
- 进入下一阶段

---

## 17.2 超时行为

如果玩家一直不左滑：

- 教学提示持续一段时间
- 例如 6～10 秒
- 然后自动淡出

但：

如果教程整体仍处于首次教学流程，可在适当时机进入下一项。

不得永久挡住画面。

---

# 18. 第二步：右滑教学

左滑教学完成或超时后：

出现：

**右滑移动**

视觉形式与左滑一致，但方向相反。

---

## 18.1 完成条件

玩家完成一次有效右滑：

- 方块正常向右移动
- 教学动画消失
- 进入旋转教学

---

# 19. 第三步：双击旋转教学

最后显示：

**双击旋转**

视觉建议：

- 半透明浅蓝色手指双击图标
- 顺时针旋转箭头
- 中文：`双击旋转`

---

## 19.1 完成条件

玩家完成一次有效双击：

- 当前方块顺时针旋转
- 教学提示消失
- 整个手势教学完成

---

# 20. 手势教学动画视觉规范

教学提示应：

- 明显
- 但不遮挡整个棋盘
- 半透明
- 浅蓝 / 冷色
- 有轻微闪烁 / 呼吸
- 图标足够大

建议：

- 动画周期较慢
- 不高频闪烁
- 不使用刺眼白光

Reduced Motion 下：

- 不做闪烁
- 使用静态图标 + 轻微透明度

---

# 21. 教学期间玩家可自由操作

强调：

在手势教学期间，玩家可以：

- 按按钮
- Hold
- Hard Drop
- Pause
- 移动
- 旋转
- 使用其他正常操作

教学不会锁住游戏。

只有当玩家执行当前教学目标动作时：

- 当前提示提前消失

否则：

- 等待超时自动消失

---

# 22. 教学是否重复

建议：

手势教学完成后：

- 记录 `gestureTutorialCompleted = true`

保存在 localStorage。

后续正常进入游戏：

- 不再自动弹出

如果玩家在 Settings 中：

- 关闭 Gesture Control
- 后来重新开启

默认不必再次教学。

---

# 23. 手动重新查看手势教学

Settings 中增加：

**重新查看手势教学**

点击后：

- 下一局重新播放教学

或：

- 当前游戏中立即重新播放

具体方式可选。

---

# 24. Gesture Control 与 Input Context

手势只在：

`GAME`

状态下生效。

以下状态必须禁用：

- MENU
- MODAL
- Settings
- Guide
- Pause
- Game Over

---

# 25. Gesture Control 与 Focus Mode

Focus Mode 开启时：

- 手势仍正常生效
- 教学提示仍可正常显示

如果 Focus Mode 隐藏了部分触控按钮：

- 不影响左右滑 / 双击旋转

---

# 26. 手势状态不应影响键盘

在 PC 窄窗口中：

即使布局进入移动 / 窄屏模式：

- 键盘仍可以正常使用
- 手势系统不能禁用键盘

原因：

用户可能只是把 PC 浏览器窗口拉窄，而不是在手机上。

---

# 27. Pointer Events 优先

手势实现建议：

优先使用：

**Pointer Events**

统一兼容：

- Touch
- Stylus
- Mouse（可选择忽略鼠标拖动手势）

不要同时维护多套复杂 Touch / Mouse 逻辑。

---

# 28. 防止手势触发页面滚动

由于只支持：

- 左滑
- 右滑
- 双击

棋盘手势区域可以针对横向手势进行控制。

但注意：

- 不应粗暴禁止整个页面所有浏览器手势
- 只在实际 GAME 棋盘区域内按需处理

---

# 29. 手势误判保护

必须处理：

- 很小的手抖不算滑动
- 垂直位移过大时，不应误判为左右滑
- 拖动开始于按钮区域时不处理
- 双击与滑动不可同时触发

建议设置：

- 最小水平距离
- 最大垂直偏差
- 双击最大时间
- 双击最大位置差

---

# 30. v2.0 新增验收测试

## 30.1 音量图标穿模

- 320px 宽度下不穿模
- 不与标题重叠
- 点击区域完整
- 不超出屏幕
- 安全区设备显示正常

---

## 30.2 窄高屏幕适配

测试：

- PC 浏览器拉成手机宽度
- 高度 900px+
- 高度 1200px+

要求：

- 下方不出现大面积无意义空白
- 棋盘会合理放大
- 整体垂直空间分布均衡

---

## 30.3 窄矮屏适配

测试：

- 360×640
- 375×667

要求：

- 不依赖纵向滚屏
- 棋盘完整
- 主要按钮完整
- 顶部栏不穿模

---

## 30.4 左滑

- 左滑可移动方块
- 不穿墙
- 不穿已有方块
- 短滑 / 长滑逻辑正常

---

## 30.5 右滑

- 右滑可移动方块
- 碰撞检测正常

---

## 30.6 双击旋转

- 双击只触发一次顺时针旋转
- Wall Kick 正常
- 不误触多次

---

## 30.7 不存在下滑手势

- 下滑不会 Soft Drop
- 快速下滑不会 Hard Drop
- 不与左右滑误判

---

## 30.8 不存在长按横移

- 长按棋盘不触发连续左 / 右移动
- 不与双击冲突

---

## 30.9 手势教学

开启 Gesture Control 后首次进入：

依次出现：

1. 左滑移动
2. 右滑移动
3. 双击旋转

要求：

- 提示图标明显
- 半透明浅蓝
- 有适度动画
- 玩家可正常游戏
- 做出目标动作后提示立即消失
- 不操作时等待一段时间自动消失
- 不会永久遮挡棋盘

---

## 30.10 教学状态保存

- 完成后后续不自动重复
- localStorage 正常记录
- 可通过“重新查看手势教学”再次触发

---

## 30.11 触控按钮收起

Gesture Control = ON 时：

- ALWAYS SHOW 正常
- AUTO 正常
- HIDE 正常

隐藏后：

- Hard Drop / Hold / Pause 等无手势替代功能仍可访问

---

## 30.12 PC 窄窗口兼容

PC 浏览器窗口拉窄时：

- 键盘仍正常
- 手势系统不会抢占键盘
- 页面布局可正确适应窄高比例

---

# 31. v2.0 核心总结

v2.0 的主题：

**真正完成移动端与窄屏游戏体验。**

重点包括：

- 修复顶部音量图标穿模
- Viewport-Aware Layout
- 不再只根据宽度切换布局
- 解决窄高窗口底部大面积空白
- Gesture Control 开关
- 左滑移动
- 右滑移动
- 双击顺时针旋转
- 明确不加入下滑操作
- 明确不加入长按横移
- 手势与按钮共存
- 触控按钮可收起
- 首次进入的非阻塞式交互教学
- 左滑 → 右滑 → 双击旋转的分步引导
- 教学完成状态保存
- 可重新查看教学

开发实现时：

**以原开发文档 + v1.1～v1.9 为基础，并以本 v2.0 覆盖所有与窄屏布局、顶部栏穿模、移动端手势、触控按钮显示策略与手势教学相关的旧要求。**
