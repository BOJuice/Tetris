# 俄罗斯方块开发文档 v2.1 — 增量更新说明

> 本文档仅记录相对于原始开发文档及 v1.1～v2.0 的新增、修改与调整内容。未提及内容继续沿用旧版本；若冲突，以 v2.1 为准。

## 1. 文案排版优化

原文：
`留一块，等合适的时机。`

修改为：
**存一块，随用随取。**

要求：
- 尽量单行呈现
- 减少不必要换行
- 中文更短、更直接
- 保持街机 HUD 的紧凑感

## 2. 最近惊喜 · RECENT 布局重新定义

不要使用：
`最近惊喜 · RECENT  GRAVITY  [查看]`

正确结构为两行。

第一行只显示：
**最近惊喜 · RECENT**

第二行：
- 左侧：最新一次 Surprise 的简称
- 右侧：小型 `查看` 按钮

示例：

```text
最近惊喜 · RECENT
GRAVITY                         [查看]
```

要求：
- 标题独占一行
- 最新惊喜简称位于下一行左侧
- 查看按钮位于同一行右侧
- 不把标题、简称和按钮挤在同一行

## 3. 最近惊喜详情面板

点击 `查看` 后打开：
**最近惊喜详情 / SURPRISE HISTORY**

显示本局已经触发过的全部 Surprise。

每条至少包含：
- 图标
- 中文名称
- 英文名称
- 简称
- 具体效果说明
- 触发顺序

可选：
- 触发时间
- 当时 Theme Phase
- 是否来自 Mystery Block

详情面板：
- 可滚动
- 可关闭
- Esc 可关闭
- 手机端可近似全屏
- PC 端可使用 Modal
- 打开时游戏保持暂停
- 关闭后仍保持暂停，直到用户主动继续

Restart 后清空本局 Surprise History。

## 4. Pause 页面文案调整

保留以下氛围文案：

- `A MOMENT OF QUIET`
- `PAUSED`
- `TAKE YOUR TIME`
- `方块会等你。`

其他功能性英文改为中文，例如：

- Resume → **继续游戏**
- Restart → **重新开始**
- Settings → **设置**
- Main Menu → **返回主菜单**
- Special Guide → **特殊内容说明**
- Focus Mode → **专注模式**

原则：
- 氛围文案可以保留英文
- 功能按钮优先中文

## 5. ONE MORE CHANCE / FIND YOUR FLOW 排版

改为尽量单行：

**ONE MORE CHANCE · FIND YOUR FLOW**

仅在窄屏确实无法容纳时才允许换行。

## 6. 手势横移改为实时跟手

现有问题：
- 滑动时不执行移动
- 直到手指 / 鼠标松开才移动

v2.1 要求：
**真正执行横移的时机必须发生在 pointermove，而不是 pointerup。**

推荐流程：

```text
pointerdown
→ 记录起点和当前活动方块位置

pointermove
→ 持续计算横向位移
→ 每超过一个格距阈值
→ 立即尝试移动一格
→ 更新参考点
→ 继续跟随

pointerup / pointercancel
→ 仅结束拖动
```

## 7. Grid Snap

视觉上允许产生“跟手”的感觉，但逻辑仍保持网格制。

要求：
- 不允许半格停留
- 每次逻辑移动均为整数格
- 每移动一格都进行碰撞检测
- 不能高速跳过墙体或已有方块

## 8. Gesture Dead Zone

加入防抖阈值。

建议：
- 起始水平移动 6～10px 内不触发横移

超过阈值后才进入有效拖动。

具体阈值可根据屏幕 DPI 调整。

## 9. 拖动中允许实时反向

如果玩家：
1. 向右拖 3 格
2. 手指仍未松开
3. 再向左拖回 2 格

方块必须实时左移回来。

不得要求：
- 先松手
- 再重新开始一次 Gesture

## 10. 碰墙后 Gesture 不失效

如果向右拖到墙边：
- 方块停止继续右移
- Gesture 仍保持 active

随后手指 / 鼠标向左拖：
- 方块必须立即可以向左回来

撞墙不能终止整个拖动状态。

## 11. Pointer Capture

建议统一使用 Pointer Events。

`pointerdown` 后合理使用：
`setPointerCapture(pointerId)`

作用：
- 手指 / 鼠标稍微离开棋盘区域时仍能持续收到 pointermove
- 直到 pointerup / pointercancel 才结束

## 12. Touch 与 Mouse 行为区分

### Touch
- 按住并左右拖动 → 实时横向移动
- 双击 / 双触 → 顺时针旋转

### Mouse
- 鼠标按住中间游戏操作区并左右拖动 → 实时横向移动
- 鼠标双击 → **立即 Hard Drop**

要求：
- 鼠标双击不得造成文本选择
- 不触发浏览器默认缩放
- 不误点其他 UI

## 13. Gesture Zone 扩大

手机端手势不必严格限制在 10×20 棋盘内部。

允许在：
**中间游戏主体区域的任意安全位置**

触发。

可包含：
- 棋盘本体
- 棋盘左右留白
- 棋盘上方少量空白
- HOLD 与 NEXT 之间的非交互区域
- 中央游戏主体背景

必须排除：
- 顶部菜单栏
- Pause / Settings / Guide
- 可点击按钮
- Slider
- 触控按钮区
- Recent Surprises 的“查看”按钮
- 其他交互控件

## 14. Gesture Control 默认开启

触摸设备上：
**Gesture Control 默认 ON。**

要求：
- 首次进入即可使用
- 首次开始游戏时播放手势教学
- 玩家可以在 Settings 中关闭

PC 宽屏：
- 键盘仍为主要输入
- 鼠标拖动 / 双击能力仍可同时存在

## 15. 左滑 / 右滑教程箭头视觉重新定义

左滑和右滑提示必须使用：

**一个单独、完整、明显的箭头图形。**

禁止：
- 文本字符 `→`
- 文本字符 `←`
- emoji 箭头
- 手指 emoji
- `>>>`
- 多个箭头排列
- 用简单线条或字符拼出的箭头

## 16. 单箭头图形规范

箭头必须具有：
- 完整箭身
- 明确三角箭头
- 较宽视觉面积
- 一眼能识别方向

推荐实现：
- Inline SVG
- CSS polygon / clip-path
- Canvas

优先推荐：
**Inline SVG**

### 左滑箭头
- 单个大型左向箭头
- 淡蓝色
- 半透明
- 轻微发光
- 呼吸 / 闪烁
- 可轻微从右向左漂移

### 右滑箭头
与左滑镜像：
- 单个完整右向箭头
- 箭身 + 三角箭头
- 淡蓝半透明
- 轻微动画

## 17. 箭头动画规范

建议：
- 箭头整体轻微平移
- 透明度缓慢呼吸
- Scale 仅做极轻变化

例如：
- 0%：Opacity 0.35，偏移 +8px
- 50%：Opacity 0.75，位置居中
- 100%：Opacity 0.35，偏移 -8px

禁止：
- 高频闪烁
- 多箭头连续排列
- emoji 风格
- 文字箭头

## 18. 双击旋转教学保持现有效果

用户已确认当前双击旋转提示可接受。

因此：
- 不重新设计该部分视觉
- 只保证 Touch 双击可顺时针旋转
- 完成后提示正确消失

## 19. 教学继续保持非阻塞

教学期间：
- 游戏继续正常运行
- 玩家可以 Hold
- Hard Drop
- Pause
- 移动
- 旋转
- 使用其他按钮

完成当前目标动作时：
- 当前提示立即消失

若始终不做目标动作：
- 等待约 6～10 秒自动淡出
- 然后进入下一提示或结束

## 20. 教学与触控按钮收起联动

Gesture Control 默认 ON。

触控按钮继续支持：
- 始终显示
- 自动
- 隐藏

AUTO 建议作为默认。

首次开始：
- 完整显示触控按钮
- 同时播放手势教学

完成教学后：
可自动收起：
- 左移
- 右移
- 旋转

继续保留：
- Hard Drop
- Hold
- Pause
- OOPS（如需要）

## 21. 教程根据启用功能动态生成

教程不能写死。

如果：
- Gesture Control 关闭

则：
- 不播放手势教程

未来若某个 Gesture 单独禁用：
- 不显示对应教学

## 22. Input Method 统一原则

Gesture Control 不是独立游戏模式。

游戏允许同时接受：
- Keyboard
- Mouse
- Touch

谁产生有效输入，就响应谁。

不要因为手势开启而禁用键盘，也不要因为键盘存在而禁用鼠标。

## 23. 窄屏布局继续修正

当前实际问题：

### 情况 A
屏幕不是特别窄，Gesture ON 时：
- 底部仍会出现较大空白

### 情况 B
屏幕非常窄，Gesture OFF 时：
- 触控按键区可能与棋盘发生重叠

v2.1 要求：
布局必须根据当前触控按钮实际显示状态重新计算。

需要考虑：
- Gesture Control ON / OFF
- Touch Controls ALWAYS SHOW
- AUTO 当前是否已收起
- HIDE
- Gesture OFF 时完整按钮区高度

## 24. Gesture ON + Buttons Hidden

如果：
- Gesture ON
- 左右 / 旋转按钮已经隐藏

则原按钮容器占用的高度必须立即释放。

可以用于：
- 放大棋盘
- 调整垂直间距
- 让主体更居中

禁止：
- 按钮隐藏但容器仍保留旧高度
- 因此出现底部大面积空白

## 25. Gesture OFF + Buttons Required

Gesture OFF 时：
- 触控按钮属于必要内容

布局必须：
1. 先测量真实按钮区高度
2. 为按钮预留完整空间
3. 再计算棋盘最大尺寸

禁止：
- 棋盘先占满
- 再用 absolute 把按钮覆盖在棋盘上

## 26. 极窄屏按钮布局

极窄屏：
- 可将按钮分两行
- 可减少 gap
- 可使用更紧凑排列

但：
- 不得与棋盘重叠
- 不得横向溢出
- 不得缩小到难以触控

## 27. v2.1 验收测试

### Recent Surprises
必须为：
第一行：
`最近惊喜 · RECENT`

第二行：
- 左侧：最新 Surprise 简称
- 右侧：小型查看按钮

点击查看：
- 可查看本局全部已触发 Surprise
- 可查看具体效果说明

### Pause 文案
必须保留：
- A MOMENT OF QUIET
- PAUSED
- TAKE YOUR TIME
- 方块会等你。

其余主要功能按钮中文化。

### 实时 Touch 拖动
- 手指按住
- 不松手
- 横向移动

方块必须在 pointermove 中实时跟随。

### 实时 Mouse 拖动
- 鼠标按住
- 左右拖动
- 不松开

方块实时横移。

### 碰墙反向
- 拖到墙边
- 保持按住
- 反方向拖动

方块立即可以返回。

### 双击行为
Touch：
- 双击 → 顺时针旋转

Mouse：
- 双击 → Hard Drop

### Gesture Zone
中间游戏主体安全区域均可触发手势。

顶栏、按钮、设置、Guide、查看按钮等不得触发。

### 教程箭头
左 / 右滑：
- 单个完整箭头图形
- 有箭身
- 有三角箭头
- 不是字符
- 不是 emoji
- 不是多个箭头
- 半透明浅蓝
- 有轻微动画

### 双击旋转教程
- 保持现有视觉
- 完成 Touch 双击后立即消失

### Gesture 默认开启
触摸设备首次进入：
- 默认 ON
- 自动首次教程
- 可在设置中关闭

### AUTO 按钮收起
完成教程后：
- 左 / 右 / 旋转按钮可收起
- Hard Drop / Hold / Pause 继续可用
- 空出的高度重新参与布局

### 窄高屏
Gesture ON + Buttons Hidden：
- 不保留旧按钮占位
- 不出现大块底部空白
- 主体合理利用高度

### 极窄屏
Gesture OFF：
- 按钮有真实空间
- 不和棋盘重叠
- 不依赖 absolute overlay
- 仍无需上下滚屏完成基本操作

## 28. v2.1 核心总结

v2.1 重点：

### 第一：信息排版更准确
- “存一块，随用随取。”
- Recent Surprises 改为标题一行 + 下一行简称与查看按钮
- Pause 功能中文化
- ONE MORE CHANCE · FIND YOUR FLOW 单行优先

### 第二：手势变为真正实时跟手
- pointermove 实时控制
- Grid Snap
- Dead Zone
- 实时反向
- 撞墙后仍可反向
- Pointer Capture

### 第三：Touch 与 Mouse 都可以直接操作
- Touch 拖动横移
- Touch 双击旋转
- Mouse 拖动横移
- Mouse 双击 Hard Drop

### 第四：教程箭头使用真正图形
- 单个完整浅蓝箭头
- 禁止文字箭头
- 禁止 emoji
- 双击旋转教程保持现状

### 第五：继续修复窄屏布局
- 布局根据 Gesture 和按钮实际显示状态重新计算
- 隐藏按钮后释放旧占位
- Gesture OFF 时为按钮预留真实空间
- 避免底部空白与按钮 / 棋盘重叠

开发实现时：

**以原开发文档 + v1.1～v2.0 为基础，并以本 v2.1 覆盖所有与 Recent Surprises 布局、Pause 文案、实时拖动、Touch / Mouse 手势、教程箭头视觉、Gesture 默认状态及窄屏触控布局相关的旧要求。**
