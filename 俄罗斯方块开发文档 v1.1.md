# 俄罗斯方块开发文档 v1.1 — 增量变更说明

> 本文档仅记录相对于原始开发文档的新增、修改与调整内容。  
> 未在本文档中提及的功能、规则、视觉设计与技术要求，全部沿用原始开发文档。

---

## 1. 旋转操作调整

### 1.1 新增双向旋转

原版本已有旋转操作，但 v1.1 明确要求支持：

- 逆时针旋转
- 顺时针旋转

两种旋转必须完全独立实现，并共用碰撞检测与 Wall Kick（墙踢）逻辑。

### 1.2 新增键盘按键

新增以下默认键位：

- `【` / `[`：逆时针旋转
- `】` / `]`：顺时针旋转

其中：

- 实际网页事件应优先兼容物理键盘 `[` 与 `]`
- 中文输入状态下若浏览器能够识别 `【` / `】`，也应正常工作
- 不应要求玩家为了旋转必须切换输入法

原有快捷键可以继续保留作为兼容操作：

- `↑`：顺时针旋转
- `Z`：逆时针旋转

### 1.3 新增旋转按钮

桌面和移动端 UI 中增加两个独立按钮：

- `⟲`：逆时针旋转
- `⟳`：顺时针旋转

要求：

- 两个按钮必须始终明确区分方向
- 点击行为必须与键盘旋转完全一致
- 必须应用相同的 Wall Kick 和碰撞检测
- 移动端按钮应满足正常触控尺寸
- 不允许只做视觉按钮而没有真实功能

---

## 2. Chill Mode 方块池扩展

### 2.1 Classic Mode 保持不变

Classic Mode 继续严格使用标准 7 种 Tetromino：

- I
- O
- T
- S
- Z
- J
- L

Classic Mode 继续使用标准 7-Bag Randomizer。

不得将新增的辅助方块、特殊方块或自适应随机机制引入 Classic Mode。

---

### 2.2 Chill Mode 增加辅助方块

为降低“缺少竖直型方块导致深坑无法处理”的挫败感，Chill Mode 不再局限于标准 7 种 Tetromino。

新增少量辅助型方块，例如：

- 3×1 直条
- 2×1 小条
- 三格 L
- 三格 T
- 竖直 3 格 + 横向 1 格的半长条
- 竖直 3 格 + 底部扩展格的靴子型
- 极低概率出现的 5 格长直条
- 极低概率出现的单格救援块

要求：

- 辅助方块只存在于 Chill Mode
- 辅助方块不能出现得过于频繁
- 标准 Tetromino 仍然是主要组成部分
- 新增方块的目的是提高容错性，而不是彻底消除堆叠压力

---

## 3. Chill Mode 随机系统调整

### 3.1 取消纯 7-Bag 作为 Chill Mode 唯一生成逻辑

Chill Mode 改为使用更大的自适应随机池：

**Adaptive Bag**

建议每轮随机池规模：

- 18～24 个方块

池中包含：

- 标准 Tetromino
- 少量辅助型方块
- 极少量特殊 / 救援型方块

---

### 3.2 新增重复抑制

当某种方块刚出现后：

- 接下来若干次生成中，该方块的权重降低
- 应明显减少同类方块连续重复出现的情况
- 但不要求完全禁止重复

目标：

避免玩家产生“来来回回就是几种方块”的明显循环感。

---

### 3.3 新增冷门补偿

如果某类重要方块长时间未出现，其生成权重逐步提升。

例如：

- I 型长条长期未出现
- 竖直辅助块长期未出现

此时可以适当增加其后续出现概率。

要求：

- 补偿应渐进发生
- 不允许直接强制下一块生成
- 玩家不应明显感觉系统在作弊

---

### 3.4 新增轻度局面辅助

Chill Mode 可以分析当前棋盘的粗略结构，例如：

- 是否存在深竖坑
- 是否存在大量单格洞
- 是否存在高危堆叠
- 是否存在适合长条处理的通道

根据局面，仅允许轻微调整下一阶段的生成权重。

例如：

存在明显深坑时，可略微提高：

- I
- 3×1 直条
- 部分竖直辅助块

要求：

- 只允许概率微调
- 不允许直接根据当前缺口精准生成“唯一正确答案”
- 保留随机性和玩家决策空间

---

## 4. 新增 Rescue Piece 机制

Chill Mode 新增：

**RESCUE PIECE**

当棋盘满足一定危险条件时，可以提高“救援型方块”的出现概率。

救援型方块可包括：

- 3×1 直条
- 2×1 小条
- 5 格长条
- 单格块
- 其他适合处理深坑的特殊形状

要求：

- Rescue Piece 不应固定触发
- 不应直接出现在 Next Queue 中作为明确“系统救场”提示
- 应保持自然随机感
- 单格块、5 格长条等强力救援块必须保持低概率

---

## 5. 新增 Lucky / Chaos Event

Chill Mode 增加低频随机事件。

事件应偶尔改变接下来几块的体验，但不能频繁打断游戏。

可实现事件包括：

### Lucky Wave

未来若干方块更容易出现较易摆放的形状。

### Tiny Blocks

接下来约 3 个方块由 2～3 格的小型辅助块组成。

### Long Boy

下一阶段提高长直条类方块出现概率。

### Mirror

某个特殊方块以镜像形式出现。

### Clear Sky

随机清除少量高风险孤立方块。

要求：

- 所有事件必须低频
- 不应连续触发
- 不应完全破坏棋盘结构
- 应作为休闲模式的变化调味，而不是主玩法

---

## 6. 新增 Perfect Clear 奖励

当玩家将整个棋盘清空时：

触发：

**PERFECT CLEAR!**

效果建议：

- 明显但短暂的视觉提示
- 专属音效
- 像素粒子 / 彩带效果
- 大量额外分数

Chill Mode 中还可以额外奖励：

- 一个随机道具
- 一次特殊事件机会
- 或其他轻量奖励

要求：

- 不得影响后续正常游戏流程
- 动画完成后立即恢复正常操作

---

## 7. 新增 Zen Mode

新增第三种游戏模式：

**ZEN MODE**

定位：

完全无压力的无限休闲模式。

主要规则：

- 不存在传统 Game Over
- 方块堆积到危险高度后，系统自动清理部分棋盘
- 不进行激进的速度提升
- 可以关闭或弱化分数系统
- 不强调等级压力
- 可以持续无限游玩
- 保留俄罗斯方块的摆放与消行核心体验

Zen Mode 适用于：

- 工作 / 学习间隙
- 不希望承担失败压力
- 单纯听音乐和放方块

模式结构因此调整为：

- CLASSIC
- CHILL
- ZEN

---

## 8. Danger 状态调整

可以保留棋盘接近顶部时的轻度危险提示，例如：

- 棋盘边缘轻微变化
- `DANGER` 字样
- 非侵入式视觉变化

但删除所有拟人化、聊天式或调侃式提示文案。

明确删除：

- “哥们，有点高了。”

以及其他类似表达。

Danger 状态不得：

- 使用强烈屏幕震动
- 突然大幅提升 BGM 速度
- 使用高频闪烁
- 制造与 Chill / Zen 定位冲突的强压迫感

---

## 9. Combo 视觉反馈扩展

Combo 除数字外，可以增加纯视觉等级提示，例如：

- 2 Combo：NICE
- 3 Combo：GREAT
- 4 Combo：AWESOME
- 5 Combo：ON FIRE
- 更高 Combo：可继续使用其他街机式称号

要求：

- 只作为视觉奖励
- 不遮挡棋盘
- 不频繁打断操作
- 不应出现聊天式提示

---

## 10. 输入与容错性补充

新增双向旋转后，应重点检查：

- 左墙附近顺时针旋转
- 左墙附近逆时针旋转
- 右墙附近顺时针旋转
- 右墙附近逆时针旋转
- 堆叠表面上的双向旋转
- Ghost Piece 与旋转后位置一致
- Hold 后的方块旋转状态正确重置
- Hard Drop 后不能继续旋转
- Lock Delay 期间旋转逻辑正常

若项目已经实现 Lock Delay，则双向旋转均应按相同规则参与 Lock Delay 刷新或限制。

---

## 11. 移动端控制调整

原移动端控制区增加两个独立旋转按钮。

推荐布局包含：

- 左移
- 右移
- Soft Drop
- Hard Drop
- Hold
- 逆时针旋转
- 顺时针旋转

禁止仅提供一个 Rotate 按钮。

如果屏幕宽度不足：

- 可以分为两行
- 可以调整按钮布局
- 不允许缩小到难以点击
- 不允许出现页面横向溢出

---

## 12. v1.1 新增验收测试

在原有验收测试基础上，增加以下检查。

### 双向旋转

- `[` 可以逆时针旋转
- `]` 可以顺时针旋转
- `【` / `】` 在可识别环境中行为正确
- `⟲` 按钮可以逆时针旋转
- `⟳` 按钮可以顺时针旋转
- 键盘和按钮结果一致
- 顺逆旋转都不会穿墙
- 顺逆旋转都不会穿过已锁定方块

### Chill 方块池

- 辅助型方块只在 Chill Mode 出现
- Classic Mode 永远保持标准 7 种 Tetromino
- 长时间游戏不会只反复出现少数几种方块
- 竖直型辅助块能够正常旋转和碰撞
- 特殊形状不会产生越界问题

### Adaptive Bag

- 重复抑制正常工作
- 冷门补偿正常工作
- 不会出现明显固定循环
- 不会因为权重计算出现 NaN / 空队列
- Next Queue 与实际生成顺序完全一致

### Rescue Piece

- 高危棋盘可以适度提高救援块概率
- 不会每次危险都直接生成最佳方块
- 不影响 Classic Mode

### Lucky / Chaos Event

- 事件不会无限叠加
- 事件结束后生成系统恢复正常
- Restart 后旧事件状态必须被清空
- 切换模式后旧事件不得残留

### Zen Mode

- 堆满后不会进入传统 Game Over
- 自动清理后游戏可以继续
- 游戏可以长期运行
- 不产生不断累积的 Timer、粒子或事件监听器

### Perfect Clear

- 棋盘清空时正确识别
- 奖励只触发一次
- 动画不会阻塞下一块生成
- Chill 奖励不会错误出现在 Classic Mode

---

## 13. v1.1 开发原则总结

此次版本调整的核心目标：

### Classic

保持纯正。

不加入：

- 辅助方块
- 自适应救援
- 道具方块
- Lucky / Chaos Event

### Chill

强调：

- 更高容错
- 更丰富的方块变化
- 较低重复感
- 较少“等不到关键方块”的挫败
- 随机但不过度恶心玩家

核心新增：

- 扩展方块池
- Adaptive Bag
- 重复抑制
- 冷门补偿
- 局面轻度辅助
- Rescue Piece
- Lucky / Chaos Event
- Perfect Clear

### Zen

强调：

- 无 Game Over
- 无限游玩
- 最低压力
- 放松体验

---

## 14. 本版本不改变的内容

除本文档明确说明的修改外，原开发文档中的以下要求继续有效：

- 基础棋盘规则
- Hold
- Ghost Piece
- Next Queue
- Combo
- Back-to-Back
- Tetris
- Chill Mode 道具
- Emergency Clear
- 音效
- Chiptune BGM
- CRT 视觉风格
- 分数系统
- 本地最高分
- 自动暂停
- 响应式布局
- Canvas 渲染
- 粒子效果
- 性能要求
- Restart / Main Menu
- 其他未被 v1.1 明确覆盖的技术与体验要求

开发实现时应：

**以原开发文档为基础，以本 v1.1 增量文档覆盖其中发生冲突的旧规则。**
